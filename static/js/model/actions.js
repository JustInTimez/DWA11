/**
 * Action object to increment the count value in the state
 * @type {object}
 */
const add = {
  type: "ADD",
};

/**
 * Action object to decrement the count value in the state
 * @type {object}
 */
const subtract = {
  type: "SUBTRACT",
};

/**
 * Action object to reset the count value in the state to zero
 * @type {object}
 */
const reset = {
  type: "RESET",
};

export { add, subtract, reset };
